import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.freebetproblackjack',
  appName: 'Free Bet Pro Blackjack',
  webDir: 'dist'
};

export default config;
